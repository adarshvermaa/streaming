export interface IWorkspace {
    name: string;
    description: string;
    createdBy?: string;
}
export interface UWorkspace {
    name: string;
    description: string;
    createdBy?: string;
    updatedAt?: Date;
}