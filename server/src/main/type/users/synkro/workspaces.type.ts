export interface IWorkspace {
  name: string;
  description: string;
  slug: string;
  createdBy: string;
  logoUrl?: string;
  bannerUrl?: string;
  websiteUrl?: string;
}
export interface UWorkspace {
  name: string;
  description: string;
  slug: string;
  createdBy: string;
  logoUrl?: string;
  bannerUrl?: string;
  websiteUrl?: string;
}
