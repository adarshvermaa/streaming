import { Router } from "express";

const WorkspacesMemberRouter = Router();

// WorkspacesMemberRouter.get('/', (req, res) => {
//     return res.status(200).json({ message: 'Workspaces Member' });
// });
export default WorkspacesMemberRouter;  